async function loadHistory(){requireLogin(); const el=document.querySelector('#history'); try{const rows=await apiRequest('getOrderHistory'); el.innerHTML=rows.length?rows.map(o=>`<div class="card flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><div class="font-semibold">${esc(o.order_id)}</div><div class="text-sm text-slate-400">${esc(o.game)} • ${Number(o.coin).toLocaleString('id-ID')} Koin</div><div class="mt-1 text-sm">${formatIDR(o.total_price)}</div></div><div class="flex items-center gap-3">${statusBadge(o.status)}<a class="btn-secondary" href="checkout.html?order=${encodeURIComponent(o.order_id)}">Detail</a></div></div>`).join(''):'<div class="card text-slate-400">Belum ada pesanan.</div>';}catch(e){toast(e.message,'error');}}
